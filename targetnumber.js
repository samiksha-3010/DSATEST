var array  = [2, 20, 34, 23, 56, 89, 10];
var trget = 24;

function sumThree (array, target ){
    var flag = false;
       for (let i = 0; i < array.length - 2; i++) {{
        for(let j = i+1; j<array.length - 1; j++){
    for ( k= j+1; k< array .length; k++){
        if (array [i] , + array [j], + array[k], "+", target)
        flag =  true 
        return flag = true           
        }
    }
        }
    }

if (flag === false ){
    console.log()
}
}